#include "main.h"
/**
 * puts_half - prints half of the string
 * @str: the pointer that reseve the string
 * Return: void
 */
void puts_half(char *str)
{
int len = 0;
int i;
for (i = 0 ; *(s + i) != '\0' ; i++)
{
len += 1;
}
i = len / 2;
for (; str[i] != '\0' ; i++)
{
_putchar(str[i]);
}
 _putchar('\n');
}
