#include "main.h"
/**
 * *_strcpy - copy string in another
 * @dest: the final string
 * @src: the coped string
 * Return: dest copy string
 */
char *_strcpy(char *dest, char *src)
{
dest = src;
return (dest);
}
